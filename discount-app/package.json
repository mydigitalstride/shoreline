{
  "name": "distributor-pricing-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "dev": "shopify app dev"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0"
  }
}
